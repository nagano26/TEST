<div>
  <h1 class="font-bold text-4xl">購入履歴</h1>
  <div>
    <%=paginate @order %>
  </div>
  <table>
    
    <% @order.product.each do |order| %>
    <tr>
      <th>商品名</th>
      <td><%=order.product.product_name(producta) %></td>
    </tr>
    <tr>
      <th>商品名（カナ）</th>
      <td><%=order.product.product_name_kana(product) %></td>
    </tr>
    <tr>
      <th>商品単価</th>
      <td><%=order.product.product_price(product) %></td>
    </tr>
    <tr>
      <th>購入数</th>
      <td></td>
    </tr>
    <tr>
      <th>購入合計額</th>
      <td></td>
    </tr>
    <tr>
      <th>購入日</th>
      <td></td>
    </tr>
  <% end %>
  </table>
</div>
